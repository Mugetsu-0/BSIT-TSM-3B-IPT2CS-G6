function showName(img) {
    console.log("Function called");
    var figcaption = image.nextElementSibling; // Get the next element, which is the figcaption
    figcaption.style.opacity = "1"; // Set the opacity to 1 to show the figcaption
}